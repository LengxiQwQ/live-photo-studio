<Window
    x:Class="LivePhotoStudio.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Live Photo Studio">

    <Grid>
        <Grid.RowDefinitions>
<RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

 <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent">
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center" Spacing="8">
                <FontIcon Glyph="&#xE8B9;" FontSize="14" />
          <TextBlock Text="Live Photo Studio" FontSize="12" FontWeight="SemiBold"/>
          </StackPanel>
        </Grid>

        <NavigationView Grid.Row="1" x:Name="NavView" 
           PaneDisplayMode="Left" 
               IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
    SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
          <NavigationViewItem Icon="Edit" Content="实况合成" Tag="Combo" IsSelected="True"/>
        <NavigationViewItem Icon="Cut" Content="实况拆解" Tag="Split"/>
     <NavigationViewItem Icon="Repair" Content="元数据修复" Tag="Repair"/>
         </NavigationView.MenuItems>

        <Frame x:Name="MainFrame" Padding="16" Background="White"/>
        </NavigationView>

        <Border Grid.Row="2" Background="White" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <Grid Margin="16,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
   <ColumnDefinition Width="Auto"/>
             </Grid.ColumnDefinitions>
  <TextBlock Grid.Column="0" Text="{x:Bind ViewModel.AppStatus, Mode=OneWay}" VerticalAlignment="Center" FontSize="12"/>
     <TextBlock Grid.Column="1" Text="By LengxiQwQ" VerticalAlignment="Center" FontSize="12" Opacity="0.5"/>
      </Grid>
        </Border>
    </Grid>
</Window>